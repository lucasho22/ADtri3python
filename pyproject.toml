[tool.poetry]
name = "repl_python3_ADtri3python"
version = "0.1.0"
description = ""
authors = ["AD1616 <<>>"]

[tool.poetry.dependencies]
python = "^3.8"
mathpy = "^0.3.0"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
